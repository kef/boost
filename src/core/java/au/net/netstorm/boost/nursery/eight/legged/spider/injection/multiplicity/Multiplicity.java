package au.net.netstorm.boost.nursery.eight.legged.spider.injection.multiplicity;

public enum Multiplicity {
    // FIX 2394 does this over-complicate things, i think there is a case for each of them
    // FIX 2394 also is there a case for OPTIONAL
    ONE, ONE_PER_GRAPH, MANY
}
